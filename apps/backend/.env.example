# NestJS runtime configuration.
PORT=3000
NODE_ENV=development

# MongoDB connection (credentials optional if using SCRAM auth).
MONGODB_URI=mongodb://localhost:27017/antifa-bookclub
MONGODB_USERNAME=antifa_reader
MONGODB_PASSWORD=changeme
