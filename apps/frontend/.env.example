# Publicly exposed API endpoint used by the Next.js application.
NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Reserved for future auth providers or feature flags (must remain NEXT_PUBLIC_*).
# NEXT_PUBLIC_FEATURE_FLAG=true
